# quick_camera_gallery

> npm install

> node server.js

Access http://localhost:3000 in a browser or phone. Upload images and they should automatically be saved to an 'uploads' directory and will be displayed on the page.
